<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand logo {% if app.request.get('_route') == 'app_main_index' %}active{% endif %}" href="{{ path('app_main_index') }}">Greedy</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <a href="#" type="button" data-toggle="modal" data-target=".bs-notif-modal-lg"><i class="fa fa-bell"></i><span class="badge">0</span></a>
                </li>

                {% if app.request.get('_route') == 'app_main_activeneeds' %}
                <li class="active">
                    <a href="{{ path('app_main_activeneeds') }}"><i class="fa fa-fire"></i> Needs <span class="sr-only">(current)</span></a>
                </li>
                {% else %}
                <li>
                    <a href="{{ path('app_main_activeneeds') }}"><i class="fa fa-fire"></i> Needs</a>
                </li>
                {% endif %}

                {% if app.request.get('_route') == 'app_chat_chatneed' or app.request.get('_route') == 'app_chat_chatfeed' %}
                <li class="active">
                    <a href="{{ path('app_chat_chatneed') }}"><i class="fa fa-envelope"></i> Messages <span class="sr-only">(current)</span></a>
                </li>
                {% else %}
                <li>
                    <a href="{{ path('app_chat_chatneed') }}"><i class="fa fa-envelope"></i> Messages</a>
                </li>
                {% endif %}

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.username }} <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('app_user_myprofil') }}">Mon compte</a></li>
                        <li><a href="{{ path('app_user_favoritcities') }}">Villes favorites</a></li>
                        <li><a href="{{ path('app_user_invitfriends') }}">Inviter des amis</a></li>
                    </ul>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('logout') }}"><i class="fa fa-power-off"></i> Deconnexion</a></li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>